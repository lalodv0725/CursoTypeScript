import { CapitalPersonalizadoPipe } from './capital-personalizado.pipe';

describe('CapitalPersonalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalPersonalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
